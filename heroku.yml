setup:
  addons:
    - plan: heroku-postgreql
      as: DATABASE
build:
  docker:
    web: Dockerfile
release:
  command:
    - bin/rails db:migrate
  image: web
run:
  web: bundle exec puma -C config/puma.rb
  